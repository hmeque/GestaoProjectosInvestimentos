package ugen.gestao.projectos.investimentos.jpa;

public class buscandoObjectoConcreto {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		/*
		EntityManagerFactory factory = Persistence.createEntityManagerFactory("projecto");
		EntityManager manager = factory.createEntityManager();
		// cuidado, use o import javax.persistence.Query
		Query query = manager.createQuery("select descricao from projecto where responsavel = 'Ugen'");
		query.setParameter("Ugen", true);

		List<Projecto> lista = query.getResultList();
		for (Projecto p : lista) {
			System.out.println(p.getDescricao());
		}
		manager.close();
*/
	}
	
	

}
