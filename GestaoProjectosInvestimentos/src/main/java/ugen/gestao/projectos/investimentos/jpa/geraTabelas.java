package ugen.gestao.projectos.investimentos.jpa;

public class geraTabelas {

	public static void main(String[] args) {
		
		/*
		Projecto project = new Projecto();
		project.setDescricao("Projecto Tindzava");
		project.setDataInicio(Calendar.getInstance());
		project.setDataFim(Calendar.getInstance());
		project.setOrcamento(2000000);
		project.setResponsavel("Ugen");
		
		Projecto project2 = new Projecto();
		project2.setDescricao("Projecto Timbila");
		project2.setDataInicio(Calendar.getInstance());
		project2.setDataFim(Calendar.getInstance());
		project2.setOrcamento(10000000);
		project2.setResponsavel("Vodacom");
		
		Projecto project3 = new Projecto();
		project3.setDescricao("Projecto Tindzava2");
		project3.setDataInicio(Calendar.getInstance());
		project3.setDataFim(Calendar.getInstance());
		project3.setOrcamento(2000000);
		project3.setResponsavel("Ugen");
		
		Projecto project4 = new Projecto();
		project4.setDescricao("Projecto Timbila2");
		project4.setDataInicio(Calendar.getInstance());
		project4.setDataFim(Calendar.getInstance());
		project4.setOrcamento(10000000);
		project4.setResponsavel("Vodacom2");
		
		EntityManagerFactory factory = Persistence.createEntityManagerFactory("projecto");
		EntityManager manager = factory.createEntityManager();
		manager.getTransaction().begin();
		manager.persist(project);
		manager.persist(project2);
		manager.persist(project3);
		manager.persist(project4);
		manager.getTransaction().commit();
		System.out.println("ID dos projecto: " + project.getNumero() + "," + project2.getNumero());
		manager.close();
		manager.close();
		factory.close();
		
		*/
	}

}
